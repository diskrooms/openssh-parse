#include <stdio.h>
#include <string.h>
#include "xmalloc.h"

int main(int argc,char *argv[]){
	//char *src="hel";
	char comment[1024];
	//desc = xstrdup(src);
	printf("%d\n", strlen(comment));
	//strlcpy(desc,src,sizeof(desc));
	//printf("%s",src[1]);
	return 0;
}

